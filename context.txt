Project: Gmail sorter / inbox cleanup
Repo: /home/rzangeneh/codebase/sorter
Remote: https://github.com/Rad-ops/gmail-sorter.git
Branch: main
Current head: 937bdb3 (Improve Gmail trash scoring and reports)

What is already done:
- Local repo initialized and pushed to GitHub.
- Gmail API credentials are in `secrets/` and ignored by git.
- Virtualenv exists at `.venv/` and required Python packages are installed there.
- Dashboard/reporting was expanded with:
  - metric cards
  - ranked bars
  - action preview
  - clickable unsubscribe links
  - unsubscribable domain grouping
  - attachment review
  - top sender bulk preview
- Year-chunked label apply runs completed successfully for:
  - 2024: 12,102 messages labeled
  - 2023: 15,653 messages labeled
  - 2022: 9,192 messages labeled
  - 2021: 8,610 messages labeled
  - 2020: 6,340 messages labeled
- Dry runs were also done for June 2026 and for 2025.
- Broad single-query runs were too slow / throttled; year-chunking worked reliably.

Current code state:
- Current version is 0.2.0 / version code 20260705.
- The code includes:
  - stronger promotional scoring using one-click unsubscribe, List-Unsubscribe, List-Id, bulk/list precedence, campaign headers, promotional sender local-parts, and strong promo subject patterns
  - `perfect_ad_match` for messages that score 100 and have multiple independent bulk/promotional signals while still respecting allowlists, important/primary labels, attachments, protected categories, and mixed-thread protection
  - body unsubscribe link extraction from text/html and text/plain payloads when `--attachment-details` is used
  - body text is not persisted; only normalized unsubscribe/preference links are written to reports
  - separate dashboard sections for header unsubscribe domains, body unsubscribe links, and header unsubscribe candidates
  - clickable unsubscribe links in dashboard tables
  - combined all-years dashboard plus per-year dashboard HTML files generated from one scan
  - progress/report filtering so reused progress files do not pollute the current query results
  - apply-stage progress output for trash and label/archive writes
  - explicit Gmail HTTP timeouts and clearer retry diagnostics
- Generated reports, checkpoints, manifests, secrets, and `.venv/` are intentionally ignored and remain local.

Important runtime notes:
- Use `.venv/bin/python` for the sorter.
- Prefer year chunks and conservative worker counts. `--workers 6` and `--sleep 0.1` have been more stable than broader/faster runs.
- For interrupted all-years trash apply runs, rerun with `--resume` but without `--refresh-existing`.
- The all-years trash apply can run for hours because trash writes are single-message Gmail API calls.
- The sorter writes reports to `reports/`, checkpoints to `data/`, and manifests to `manifests/`.
- Trash stages use Gmail's Trash API, not permanent delete.
- Final all-years run should use `in:anywhere -in:trash` and a dedicated all-years progress file.

Useful commands already validated:
```bash
cd /home/rzangeneh/codebase/sorter
.venv/bin/python src/gmail_sorter.py --help
```

Suggested next focus:
- Run the final all-years trash apply command from `command to resume.txt`.
- If Gmail throttles, reduce workers to 4 and increase sleep to 0.2.
